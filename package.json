{
  "name": "aws-nodejs-layer",
  "version": "1.20.0",
  "description": "The CDK Construct Library to create nodejs Lambda functions with layers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "pretest": "tslint -p . && tsc",
    "clean": "rm -rf dist",
    "build": "tsc",
    "prepack": "npm run clean && npm run build"
  },
  "engines": {
    "node": ">= 10.3.0"
  },
  "dependencies": {
    "@aws-cdk/core": "^1.20.0"
  },
  "peerDependencies": {
    "@aws-cdk/aws-lambda": "^1.20.0",
    "@aws-cdk/core": "^1.20.0"
  },
  "devDependencies": {
    "@aws-cdk/aws-lambda": "^1.20.0",
    "@types/node": "^12.12.21",
    "ts-node": "^8.5.4",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  }
}
